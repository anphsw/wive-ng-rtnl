# For sadko
) Add KLogd, SysLogd and  RemoteSysLogIP variables (Need add in webui system log page).
) Add RemoteManagementPort variable for set goahead port(default 80)(Need add in webui misc page).
) Need add disconnect selected STA in AP mode (WebUI)
iwpriv ra0 set DisConnectSta=MAC
iwpriv ra0 set DisConnectAllSta=1

) Add /proc/rt3052/esw_cnt for traffic statistic per switch port.(need add in WebUI).


# For me
) goahead need bind only lan_if if managment in only lan mode
#need fix
) Add skb reuse support.
