# For sadko
) Add Nat Mode select variable nat_mode=0/1/2(Linux/Fcone/Rcone, default 1). Need add in WebUI in misc.
) Need add UDPXYPort select to WebUI (default 81, user must be select only 81 or 1024..65535 range)
) Rewrite profiles managment code in ethernet converter mode. (in progress)
) Need rewrite QoS page (remove ralink QoS and add new types QoS)
) Add /proc/rt3052/esw_cnt for traffic statistic per switch port.(need add in WebUI).

# For me
need?) goahead need bind only lan_if if managment in only lan mode
suspended) xl2tpd now support new l2tp kernel module introduced in 2.6.32.
#need fix
buggy) Add skb reuse support.
