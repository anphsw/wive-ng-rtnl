.EXPORT_ALL_VARIABLES:

CONSOLE_BAUD_RATE = 57600

LOPT += -Os                  # library is alway 02
UOPT += -Os                  # user apps are always Os

MODEL_NAME		   = RTNL
KERNEL_VER		   = "1.2"
FS_VER			   = "0.0"
PRODUCTID		   = "RT-NL"
HWL1			   = "1.0"
HWH1			   = "0.0"
HWL2			   = "0.0"
HWH2			   = "0.0"
HWL3			   = "0.0"
HWH3			   = "0.0"
HWL4			   = "0.0"
HWH4			   = "0.0"

CONFIG_NOUSB		   = y

MACHINE			   = mips
ARCH		           = mips
ROOTDIR			   = /opt/Wive-RTNL
FIRMROOT        	   = $(ROOTDIR)
TOOLCHAINDIR          	   = $(FIRMROOT)/toolchain
LINUXDIR		   = $(FIRMROOT)/linux-2.6.21.x
LINUXINCDIR                = $(LINUXDIR)/include
CONFIG_CROSS_COMPILER_PATH = $(TOOLCHAINDIR)/bin

include $(ROOTDIR)/vendors/config/mips/config.arch

#added by Steven Liu
CFLAGS  +=  -I$(ROOTDIR)
CFLAGS  +=  
#--

############################################################################
