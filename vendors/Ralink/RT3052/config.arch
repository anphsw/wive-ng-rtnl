.EXPORT_ALL_VARIABLES:

include $(ROOTDIR)/vendors/config/mips/config.arch

CONSOLE_BAUD_RATE = 57600

CFLAGS  +=  -I$(ROOTDIR)
CFLAGS  +=  -Os
UOPT += -Os                  # user apps are always Os
LOPT += -Os                  # library is alway 02

MODEL_NAME		   = RTNL
KERNEL_VER		   = "1.2"
FS_VER			   = "0.0"
PRODUCTID		   = "RT-NL"
HWL1			   = "1.0"
HWH1			   = "0.0"
HWL2			   = "0.0"
HWH2			   = "0.0"
HWL3			   = "0.0"
HWH3			   = "0.0"
HWL4			   = "0.0"
HWH4			   = "0.0"

CONFIG_NOUSB		   = y

MACHINE			   = mips
ARCH		           = mips
ROOTDIR			   = /opt/Wive-RTNL
FIRMROOT        	   = $(ROOTDIR)
TOOLCHAINDIR          	   = $(FIRMROOT)/toolchain
LINUXINCDIR                = $(FIRMROOT)/linux-2.6.21.x/include
CONFIG_CROSS_COMPILER_PATH = $(TOOLCHAINDIR)/bin
